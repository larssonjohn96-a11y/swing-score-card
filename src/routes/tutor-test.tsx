import { createFileRoute, Link } from "@tanstack/react-router";
import { ArrowLeft, ArrowRight, BarChart3, Check, RotateCcw, Trophy, X, XIcon } from "lucide-react";
import { useState } from "react";
import { useHideBottomNav } from "@/lib/bottom-nav-visibility";
import { LIGHT_SURFACE } from "./8-bollar";
import { TUTOR_PUTTS,TUTOR_ROLLING_WINDOW,bestRollingAverage,formatTutorAverage,loadTutorSessions,recentAverage,saveTutorSession } from "@/lib/tutor-test";
export const Route=createFileRoute("/tutor-test")({head:()=>({meta:[{title:"Tutor – Putting Start Line | SG4"}]}),component:TutorTestPage});
type Phase="intro"|"test"|"result";
function TutorTestPage(){useHideBottomNav(true);const[phase,setPhase]=useState<Phase>("intro");const[results,setResults]=useState<boolean[]>([]);function start(){setResults([]);setPhase("test")}function register(hit:boolean){const next=[...results,hit];setResults(next);if(next.length>=TUTOR_PUTTS){saveTutorSession(next);setPhase("result")}}
if(phase==="intro")return <main style={LIGHT_SURFACE} className="mx-auto flex min-h-[100dvh] w-full max-w-md flex-col bg-background px-5 pb-5 pt-4 text-foreground"><div className="flex items-center justify-between"><Link to="/traning" search={{category:"putting"}} className="inline-flex h-9 w-9 items-center justify-center rounded-full border border-border bg-card"><ArrowLeft className="h-4 w-4"/></Link><Link to="/tutor-test-historik" className="inline-flex items-center gap-1.5 rounded-full border border-border bg-card px-3 py-1.5 text-xs text-muted-foreground"><BarChart3 className="h-3.5 w-3.5"/> Progress</Link></div><p className="mt-5 text-[10px] font-semibold uppercase tracking-[0.22em] text-muted-foreground">Putting · Startlinje · Träningstest</p><h1 className="mt-2 font-display text-3xl leading-none">Tutor</h1><p className="mt-2.5 text-[13px] leading-relaxed text-muted-foreground">Slå 10 puttar genom Putting Tutor. Varje boll som startar genom gaten ger 1 poäng. Testet isolerar din startlinje och repeterbarhet.</p><section className="mt-5 rounded-2xl border border-border bg-card p-4"><p className="text-[10px] font-semibold uppercase tracking-[0.16em] text-muted-foreground">Upplägg</p><div className="mt-3 space-y-2 text-sm"><div className="flex items-center justify-between border-b border-border pb-2"><span className="text-muted-foreground">Antal puttar</span><span className="font-semibold">10</span></div><div className="flex items-center justify-between border-b border-border pb-2"><span className="text-muted-foreground">Genom Tutor</span><span className="font-semibold text-primary">1 poäng</span></div><div className="flex items-center justify-between"><span className="text-muted-foreground">Missar gaten</span><span className="font-semibold">0 poäng</span></div></div></section><section className="mt-3 rounded-2xl border border-primary/20 bg-tint p-4"><p className="text-[10px] font-semibold uppercase tracking-[0.16em] text-primary">Viktigaste KPI:n</p><p className="mt-2 font-display text-2xl leading-none">20-testers snitt</p><p className="mt-2 text-xs leading-relaxed text-muted-foreground">Appen följer ditt rullande snitt över de senaste 20 testerna.</p></section><button onClick={start} className="mt-auto flex w-full items-center justify-center gap-2 rounded-2xl bg-primary py-4 font-display text-xl text-primary-foreground">Starta test <ArrowRight className="h-5 w-5"/></button></main>;
if(phase==="test"){const shot=results.length,score=results.filter(Boolean).length;return <main style={LIGHT_SURFACE} className="mx-auto flex min-h-[100dvh] w-full max-w-md flex-col bg-background px-5 pb-6 text-foreground"><div className="flex items-center justify-between pt-[max(1rem,env(safe-area-inset-top))]"><span className="text-sm font-semibold">Putt {shot+1} av {TUTOR_PUTTS}</span><Link to="/traning" search={{category:"putting"}} className="flex items-center gap-1 rounded-full border border-border bg-card px-3 py-1.5 text-xs font-semibold text-muted-foreground"><X className="h-3.5 w-3.5"/> Avbryt</Link></div><div className="mt-4 rounded-2xl border border-border bg-card p-3 shadow-sm"><div className="grid grid-cols-10 gap-1.5">{Array.from({length:TUTOR_PUTTS},(_,index)=>{const value=results[index];return <div key={index} className={`h-2 rounded-full ${value===true?"bg-primary":value===false?"bg-destructive/60":index===shot?"bg-primary/35":"bg-muted"}`}/>})}</div></div><section className="mt-4 flex h-[190px] flex-col items-center justify-center rounded-3xl border border-border bg-card px-6 text-center shadow-sm"><p className="text-[10px] font-semibold uppercase tracking-[0.2em] text-muted-foreground">Startlinje</p><p className="mt-3 font-display text-4xl leading-none">Genom Tutor?</p><p className="mt-3 max-w-[260px] text-sm leading-relaxed text-muted-foreground">Registrera bara om bollen startade genom gaten.</p></section><div className="mt-4 grid grid-cols-2 gap-3"><button onClick={()=>register(true)} className="flex h-[132px] flex-col items-center justify-center rounded-3xl border border-primary/20 bg-tint text-primary shadow-sm active:scale-95"><Check className="h-7 w-7"/><span className="mt-3 font-display text-2xl leading-none">Genom</span><span className="mt-1 text-xs">1 poäng</span></button><button onClick={()=>register(false)} className="flex h-[132px] flex-col items-center justify-center rounded-3xl border border-border bg-card shadow-sm active:scale-95"><XIcon className="h-7 w-7 text-muted-foreground"/><span className="mt-3 font-display text-2xl leading-none">Miss</span><span className="mt-1 text-xs text-muted-foreground">0 poäng</span></button></div><div className="mt-3 flex items-center justify-between rounded-2xl border border-border bg-card px-4 py-3 text-sm"><span className="text-muted-foreground">Hittills</span><span className="font-semibold tabular-nums">{score} / {shot}</span></div></main>}
const score=results.filter(Boolean).length,sessions=loadTutorSessions(),latestAverage=recentAverage(sessions),rollingCount=Math.min(sessions.length,TUTOR_ROLLING_WINDOW),bestAverage=bestRollingAverage(sessions),isPerfect=score===TUTOR_PUTTS;
return <main style={LIGHT_SURFACE} className="mx-auto min-h-screen w-full max-w-md bg-background px-5 pb-16 pt-6 text-foreground"><header className="flex items-center justify-between"><div className="flex items-center gap-3"><Link to="/traning" search={{category:"putting"}} className="inline-flex h-10 w-10 items-center justify-center rounded-full border border-border bg-card"><ArrowLeft className="h-4 w-4"/></Link><div><h1 className="text-lg font-semibold leading-tight">Tutor</h1><p className="text-xs text-muted-foreground">Resultat</p></div></div><Link to="/tutor-test-historik" className="inline-flex items-center gap-1.5 rounded-full border border-border bg-card px-3 py-2 text-xs font-semibold text-muted-foreground"><BarChart3 className="h-3.5 w-3.5"/> Progress</Link></header><section className="mt-5 rounded-3xl border border-border bg-card p-6 text-center"><p className="text-[11px] font-semibold uppercase tracking-[0.2em] text-muted-foreground">Senaste test</p><p className="mt-2 font-display text-6xl leading-none text-primary">{score}<span className="ml-2 text-xl text-muted-foreground">/ 10</span></p>{isPerfect?<p className="mx-auto mt-4 inline-flex items-center gap-1.5 rounded-full bg-tint px-3 py-1.5 text-xs font-semibold text-primary"><Trophy className="h-3.5 w-3.5"/> Perfekt test</p>:<p className="mt-4 text-xs text-muted-foreground">{score*10}% genom Tutor</p>}</section><section className="mt-3 rounded-3xl border border-primary/20 bg-tint p-5"><p className="text-[11px] font-semibold uppercase tracking-[0.16em] text-primary">{sessions.length>=TUTOR_ROLLING_WINDOW?"20-testers snitt":`Snitt hittills · ${rollingCount} test`}</p><p className="mt-2 font-display text-5xl leading-none text-primary">{formatTutorAverage(latestAverage)}</p><p className="mt-2 text-xs text-muted-foreground">{sessions.length>=TUTOR_ROLLING_WINDOW?"Rullande snitt av dina senaste 20 test.":`20-testers KPI låses upp efter ${TUTOR_ROLLING_WINDOW-sessions.length} test till.`}</p></section><div className="mt-3 grid grid-cols-2 gap-3"><div className="rounded-2xl border border-border bg-card p-4"><p className="text-[11px] font-semibold uppercase tracking-[0.14em] text-muted-foreground">Träffprocent</p><p className="mt-2 font-display text-3xl leading-none">{score*10}%</p><p className="mt-1 text-xs text-muted-foreground">Det här testet</p></div><div className="rounded-2xl border border-border bg-card p-4"><p className="text-[11px] font-semibold uppercase tracking-[0.14em] text-muted-foreground">PB · 20-test snitt</p><p className="mt-2 font-display text-3xl leading-none">{bestAverage===null?"–":formatTutorAverage(bestAverage)}</p><p className="mt-1 text-xs text-muted-foreground">{bestAverage===null?"Kräver 20 test":"Bästa rullande snitt"}</p></div></div><button onClick={start} className="mt-6 flex w-full items-center justify-center gap-2 rounded-2xl bg-primary py-4 font-display text-xl text-primary-foreground"><RotateCcw className="h-5 w-5"/> Kör igen</button><Link to="/tutor-test-historik" className="mt-3 flex w-full items-center justify-center gap-2 rounded-2xl border border-border bg-card py-4 font-semibold"><BarChart3 className="h-4 w-4"/> Se utveckling</Link><Link to="/traning" search={{category:"putting"}} className="mt-3 flex w-full items-center justify-center gap-2 rounded-2xl border border-border bg-card py-4 text-sm font-semibold text-muted-foreground"><ArrowLeft className="h-4 w-4"/> Tillbaka till Putting</Link></main>}
