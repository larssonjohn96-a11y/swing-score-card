name: Wire SG4 engine

"on":
  push:
    paths:
      - .github/workflows/wire-sg4-engine.yml

permissions:
  contents: write

jobs:
  wire:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - name: Apply SG4 engine final fix
        run: python scripts/wire_sg4_engine.py

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - run: bun install --frozen-lockfile
      - run: bun run build
      - run: bunx vitest run src/lib/sg4-engine.test.ts

      - name: Commit final fix
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/lib/sg4-engine.ts src/routes/traning.tsx
          git commit -m "Preserve training labels in SG4 engine"
          git push origin main
