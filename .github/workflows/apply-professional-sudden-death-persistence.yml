name: Apply professional sudden death and persistence

on:
  push:
    paths:
      - scripts/patch-professional-sudden-death-persistence.py
      - .github/workflows/apply-professional-sudden-death-persistence.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0
      - run: python scripts/patch-professional-sudden-death-persistence.py
      - name: Commit changes and clean patch files
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/routes/match.tsx
          git rm scripts/patch-professional-sudden-death-persistence.py .github/workflows/apply-professional-sudden-death-persistence.yml
          if git diff --cached --quiet; then exit 0; fi
          git commit -m "Polish sudden death and persist active matches"
          git push origin HEAD:main
